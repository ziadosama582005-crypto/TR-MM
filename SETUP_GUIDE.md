# 🚀 دليل النشر على Render

## خطوات النشر:

### 1️⃣ **إنشاء حساب على Render**
- اذهب إلى [render.com](https://render.com)
- سجل دخول أو أنشئ حساب جديد

### 2️⃣ **ربط مستودع GitHub**
- اذهب إلى Dashboard
- اختر "New +" ثم "Web Service"
- اختر "GitHub"
- ابحث عن `TR-MM` واختره
- اضغط "Connect"

### 3️⃣ **تحديد الإعدادات الأساسية**
```
Name: telegram-bot-app (أي اسم تفضله)
Runtime: Python
Build Command: pip install -r requirements.txt
Start Command: gunicorn app:app
```

### 4️⃣ **إضافة متغيرات البيئة (الخطوة الأهم!)**

في صفحة "Environment":
1. اضغط "Add Environment Variable"
2. أضف كل من:

#### **BOT_TOKEN** (رمز البوت من BotFather)
```
Key: BOT_TOKEN
Value: 123456789:ABCdefGHIjklmnoPQRstuvWxyz...
```

#### **SITE_URL** (رابط تطبيقك على Render)
```
Key: SITE_URL
Value: https://telegram-bot-app.onrender.com
(سيعطيك Render الرابط الفعلي بعد النشر الأول)
```

#### **FIREBASE_CREDENTIALS** (بيانات Firebase)
```
Key: FIREBASE_CREDENTIALS
Value: {"type":"service_account","project_id":"your-project","private_key":"...","client_email":"..."}
```

#### **SECRET_KEY** (مفتاح سري عشوائي)
```
Key: SECRET_KEY
Value: your-super-secret-key-change-this-in-production-12345
```

#### **ADMIN_PASS** (كلمة مرور لوحة التحكم - اختياري)
```
Key: ADMIN_PASS
Value: admin123
```

### 5️⃣ **النشر**
اضغط "Deploy" وانتظر انتهاء النشر (قد يستغرق 2-5 دقائق)

---

## 🔧 **الحصول على البيانات المطلوبة:**

### 🤖 **BOT_TOKEN من BotFather:**
```
1. افتح Telegram وابحث عن @BotFather
2. أرسل: /newbot
3. اتبع التعليمات
4. ستحصل على: 123456789:ABCdefGHIjklmnoPQRstuvWxyz...
```

### 🔥 **FIREBASE_CREDENTIALS:**
```
1. اذهب إلى Firebase Console
2. اختر مشروعك
3. اذهب إلى Project Settings ⚙️
4. اذهب إلى Service Accounts
5. اضغط "Generate New Private Key"
6. انسخ كل محتوى JSON file واستخدمه كـ value
```

---

## ⚠️ **نصائح مهمة:**

1. **لا تنسى تحديث SITE_URL**:
   - بعد نشر التطبيق، Render سيعطيك URL نهائي
   - قم بتحديث SITE_URL بهذا الرابط
   - أعد النشر (Deploy)

2. **اختبر الـ webhook**:
   ```
   https://your-app-url.onrender.com/set_webhook
   ```
   يجب أن تحصل على:
   ```
   Webhook set to https://your-app-url.onrender.com/webhook
   ```

3. **مراقبة السجلات**:
   - في Render Dashboard
   - اختر Web Service
   - اضغط "Logs"
   - ستشاهد أي أخطاء أو رسائل

---

## 🐛 **حل المشاكل الشائعة:**

### ❌ "ModuleNotFoundError: No module named 'telebot'"
- **السبب**: لم يتم تثبيت المكتبات
- **الحل**: تأكد من وجود `requirements.txt` صحيح

### ❌ "Firebase credentials error"
- **السبب**: بيانات Firebase غير صحيحة
- **الحل**: تحقق من JSON الكامل بدون حذف أي حرف

### ❌ "webhook error"
- **السبب**: SITE_URL غير صحيح
- **الحل**: استخدم الرابط الذي أعطاك Render

### ❌ "Port in use"
- **السبب**: المنفذ مشغول
- **الحل**: غير المنفذ في render.yaml إلى 3000 أو 8000

---

## ✅ **التحقق من العمل:**

بعد النشر، جرب:
1. افتح البوت في Telegram
2. أرسل: `/start`
3. يجب أن تحصل على رسالة ترحيب
4. اختبر الأوامر الأخرى

---

## 📞 **للمساعدة:**

إذا واجهت مشكلة:
1. افتح Logs في Render
2. ابحث عن رسالة الخطأ
3. شارك الخطأ معي 😊
