# 🤖 Telegram Bot App - سوق البوت الآمن

تطبيق متكامل لبيع وشراء الاشتراكات عبر Telegram مع نظام حماية أموال آمن.

## ✨ المميزات

- 🛒 منصة شراء وبيع منتجات آمنة
- 💳 نظام شحن رصيد مع مفاتيح
- 🔐 تحقق آمن من هوية المستخدمين
- 🎯 واجهة مستخدم حديثة وجميلة
- 📊 لوحة تحكم للمالك
- 🤖 بوت Telegram متكامل
- 🔥 قاعدة بيانات Firebase

## 🚀 البدء السريع

### متطلبات
- Python 3.8+
- حساب Telegram Bot (من @BotFather)
- حساب Firebase
- حساب Render (للنشر)

### التثبيت المحلي

```bash
# نسخ المستودع
git clone https://github.com/yourusername/TR-MM.git
cd TR-MM

# تثبيت المكتبات
pip install -r requirements.txt

# إنشاء ملف .env
cp .env.example .env

# تحرير .env وإضافة البيانات
nano .env

# التشغيل
python app.py
```

### النشر على Render

1. **اذهب إلى [render.com](https://render.com)**

2. **أنشئ Web Service جديد:**
   - اضغط "New Web Service"
   - اختر GitHub
   - اربط مستودع TR-MM

3. **أضف متغيرات البيئة:**

| المتغير | القيمة | مثال |
|--------|--------|------|
| `BOT_TOKEN` | رمز البوت من BotFather | `123456789:ABCdefGHIjklmn...` |
| `SITE_URL` | رابط التطبيق على Render | `https://telegram-bot-app.onrender.com` |
| `FIREBASE_CREDENTIALS` | JSON كامل من Firebase | `{"type":"service_account",...}` |
| `SECRET_KEY` | مفتاح سري عشوائي | `super-secret-key-12345` |
| `ADMIN_PASS` | كلمة مرور الآدمن | `admin123` |

4. **اضغط Deploy وانتظر انتهاء النشر**

5. **اختبر الـ webhook:**
```
https://your-app.onrender.com/set_webhook
```

## 📋 الأوامر المتاحة

### أوامر المستخدم
- `/start` - الترحيب والبدء
- `/code` - الحصول على كود تحقق
- `/my_id` - عرض معرفك
- `/web` - فتح السوق

### أوامر المالك فقط
- `/add_admin ID` - إضافة مشرف
- `/remove_admin ID` - حذف مشرف
- `/list_admins` - قائمة المشرفين
- `/add_product` - إضافة منتج
- `/توليد AMOUNT COUNT` - توليد مفاتيح شحن
- `/add USER_ID AMOUNT` - شحن رصيد مستخدم

## 🏗️ هيكل المشروع

```
TR-MM/
├── app.py                 # التطبيق الرئيسي
├── requirements.txt       # المكتبات المطلوبة
├── Procfile              # إعدادات Heroku
├── render.yaml           # إعدادات Render
├── .env.example          # مثال للمتغيرات
├── SETUP_GUIDE.md        # دليل التثبيت
└── deploy.sh             # script النشر
```

## 🔒 الأمان

- Firebase لتخزين البيانات الآمن
- Telegram WebApp للمصادقة الآمنة
- أكواد التحقق المؤقتة
- تشفير البيانات المحساسة
- حماية CSRF على الطلبات

## 📞 الدعم

إذا واجهت مشاكل:

1. **تحقق من Render Logs:**
   ```
   Dashboard → Web Service → Logs
   ```

2. **تحقق من متغيرات البيئة:**
   - تأكد من وجود جميع المتغيرات المطلوبة
   - تحقق من عدم وجود مسافات إضافية

3. **اختبر الاتصال:**
   ```
   curl https://your-app.onrender.com/health
   ```

## 📄 الترخيص

هذا المشروع مفتوح المصدر - استخدمه بحرية

## 👨‍💻 المساهمة

نرحب بالمساهمات! 🎉

---

**تم إنشاؤه بـ ❤️ لسهولة عملية البيع والشراء الآمنة**
