# 📦 نشر التطبيق على Render - دليل مفصل

## الخطوة 1️⃣: التحضير

تأكد من أن لديك:
- ✅ حساب GitHub مع المستودع
- ✅ حساب Render (مجاني)
- ✅ رمز BOT_TOKEN من BotFather
- ✅ بيانات Firebase JSON

---

## الخطوة 2️⃣: إنشاء Web Service على Render

### أ) اذهب إلى Dashboard
```
https://dashboard.render.com
```

### ب) أنشئ Web Service جديد
1. اضغط **"New +"** في الأعلى
2. اختر **"Web Service"**
3. اختر **"GitHub"** للربط

### ج) اختر المستودع
1. ابحث عن `TR-MM`
2. اضغط **"Connect"**

---

## الخطوة 3️⃣: إعدادات الخدمة

**ملء النموذج:**

```
Name:                telegram-bot-app (أي اسم)
Runtime:             Python
Build Command:       pip install -r requirements.txt
Start Command:       gunicorn app:app
Instance Type:       Free (مجاني)
```

اضغط **"Create Web Service"**

---

## الخطوة 4️⃣: إضافة متغيرات البيئة (الأهم!)

### ⚠️ **هذه الخطوة حاسمة - بدونها البوت لن يعمل**

1. **بعد الإنشاء، ستدخل صفحة التفاصيل**
2. **انزل لأسفل إلى "Environment"**
3. **اضغط "Add Environment Variable"** لكل متغير:

---

### **1️⃣ BOT_TOKEN**

```
Key:   BOT_TOKEN
Value: 123456789:ABCdefGHIjklmnoPQRstuvWxyz1234567890ABC
```

**كيفية الحصول عليه:**
- افتح Telegram
- ابحث عن `@BotFather`
- أرسل: `/newbot`
- اتبع التعليمات
- ستحصل على رمز مثل: `123456789:ABCD...`

---

### **2️⃣ FIREBASE_CREDENTIALS**

```
Key:   FIREBASE_CREDENTIALS
Value: {"type":"service_account","project_id":"my-project",...}
```

**كيفية الحصول عليه:**
1. اذهب إلى [Firebase Console](https://console.firebase.google.com)
2. اختر مشروعك
3. اذهب إلى **⚙️ Project Settings**
4. اختر tab **"Service Accounts"**
5. اضغط **"Generate New Private Key"**
6. سيُحمل ملف JSON - **انسخ محتواه بالكامل** (بدون حذف أي حرف!)
7. الصقه في حقل Value

**⚠️ مهم جداً:**
- لا تحذف أي حروف من JSON
- تأكد من عدم وجود أسطر جديدة
- يجب أن يكون كل شيء في سطر واحد

---

### **3️⃣ SITE_URL**

```
Key:   SITE_URL
Value: https://telegram-bot-app.onrender.com
```

**ملاحظة:**
- بعد النشر الأول، Render سيعطيك رابط فريد
- استبدل الرابط بالفعلي من Render
- مثال: `https://my-awesome-app-abc123.onrender.com`

---

### **4️⃣ SECRET_KEY**

```
Key:   SECRET_KEY
Value: my-super-secret-key-12345-abcdefghij-change-this
```

**يمكنك استخدام أي string طويل وعشوائي**

---

### **5️⃣ ADMIN_PASS** (اختياري)

```
Key:   ADMIN_PASS
Value: admin123
```

---

## الخطوة 5️⃣: نشر التطبيق

1. **انتظر حتى ينتهي البناء** (Build)
   - ستشاهد "Building..." ثم "Deploying..."
   - قد يستغرق 2-5 دقائق

2. **تحقق من الحالة:**
   - إذا أصبحت "Live" = ✅ نجح!
   - إذا أصبحت "Failed" = ❌ فشل (شاهد الأخطاء)

---

## الخطوة 6️⃣: اختبار البوت

### أ) تفعيل Webhook

افتح في المتصفح:
```
https://telegram-bot-app.onrender.com/set_webhook
```

يجب أن تحصل على:
```
Webhook set to https://telegram-bot-app.onrender.com/webhook
```

### ب) اختبر البوت

1. افتح Telegram
2. ابحث عن البوت الخاص بك
3. أرسل: `/start`
4. يجب أن تحصل على رسالة ترحيب

---

## ❌ حل المشاكل

### **مشكلة: "Build failed"**

**الحل:**
1. شاهد Logs في Render
2. تحقق من المكتبات في `requirements.txt`
3. تأكد من صحة `app.py`

---

### **مشكلة: "Runtime Error"**

**الحل:**
1. اذهب إلى **Logs**
2. ابحث عن رسالة الخطأ
3. الخطأ غالباً يكون:
   - `BOT_TOKEN` غير صحيح
   - `FIREBASE_CREDENTIALS` ناقص

---

### **مشكلة: "البوت لا يستجيب"**

**الحل:**
1. تأكد من تفعيل Webhook:
   ```
   https://your-app.onrender.com/set_webhook
   ```
2. تحقق من أن البوت "Live" (أخضر)
3. انتظر دقيقة وحاول مجدداً

---

### **مشكلة: "Firebase Connection Error"**

**الحل:**
1. تحقق من `FIREBASE_CREDENTIALS` كامل بدون أخطاء
2. تأكد من نسخه **بالكامل** بدون حذف أي حرف
3. يجب أن يبدأ بـ `{"type":"service_account"...`
4. يجب أن ينتهي بـ `...cert_url":"..."}`

---

## 🎉 تم!

البوت يجب أن يعمل الآن! 

**للتحقق:**
- افتح البوت في Telegram
- أرسل `/start`
- اختبر الأوامر الأخرى

---

## 📊 مراقبة التطبيق

### عرض السجلات (Logs)

1. اذهب إلى Dashboard
2. اختر Web Service
3. اضغط على **"Logs"** في الجانب الأيسر
4. ستشاهد جميع الرسائل والأخطاء

### إعادة نشر

إذا عدلت الكود على GitHub:
1. اضغط **"Manual Deploy"** أو
2. Render سيعيد النشر تلقائياً

---

## 💡 نصائح

1. **اترك الـ cron job مفعل** - يحافظ على التطبيق "مستيقظ"
2. **استخدم Logs دائماً لتتبع الأخطاء**
3. **احفظ Webhook URL** في مكان آمن
4. **غير SECRET_KEY** في الإنتاج

---

**هل تحتاج مساعدة؟ 📞**
- اكتب رسالة خطأ كاملة
- شارك Logs من Render
- سأساعدك في الحل! 😊
